# references:
# * http://www.objc.io/issue-6/travis-ci.html
# * https://github.com/supermarin/xcpretty#usage

language: objective-c
# cache: cocoapods
# podfile: Example/Podfile
# before_install:
# - gem install cocoapods # Since Travis is not always on latest version
# - pod install --project-directory=Example
before_install:
    - brew update
    - brew install xctool
script:
#	- set -o pipefail && xcodebuild test -workspace Example/spark-sdk.xcworkspace -scheme Spark-SDK -sdk iphonesimulator ONLY_ACTIVE_ARCH=NO | xcpretty -c
	- xctool -workspace Spark-SDK.xcworkspace -sdk iphonesimulator -arch i386 -scheme Spark-SDK test
	- pod lib lint --quick
